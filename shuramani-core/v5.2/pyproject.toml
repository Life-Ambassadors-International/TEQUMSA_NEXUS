[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
[project]
name = "shuramani_core"
version = "5.2.0"
requires-python = ">=3.9"
[tool.maturin]
features = ["pyo3/extension-module"]
